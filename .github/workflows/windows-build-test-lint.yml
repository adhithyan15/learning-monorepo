name: Windows Build Test Lint
run-name: Building and Testing changes on Windows 🚀

on: [push]

jobs:
  Windows-Build-Test-Lint:
    runs-on: windows-latest

    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🪟 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The branch is ${{ github.ref }} from ${{ github.repository }}."

      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Set up Ruby with DevKit
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.3

      - name: Setup MSVC environment
        uses: microsoft/setup-msbuild@v2

      - name: Scream Test for MSVC
        run: |
          echo "Attempting to compile with MSVC..."
          cl /? # Simple test to see if cl.exe is found
        shell: cmd

      - name: Install dependencies and run recursive build
        shell: pwsh
        run: |
          # --------------------------
          # Install dev tools
          # --------------------------
          go version
          choco install rust -y
          choco install llvm -y
          clang -v
          choco install mingw -y
          gcc --version
          choco install erlang -y
          choco install gnucobol -y
          choco install strawberryperl -y
          choco install haskell-dev -y
          choco install openjdk -y
          choco install zig -y
          dotnet --help
          choco install lua -y
          choco install r -y
          gem install rspec
          npm install -g typescript
          irm https://deno.land/install.ps1 | iex
          npm install -g bun
          cmake --version

          # --------------------------
          # Run monorepo build system
          # --------------------------
          ruby code\programs\ruby\monorepo_build\build.rb
